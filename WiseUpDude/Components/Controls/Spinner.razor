@* Spinner.razor *@
<div class="spinner @(IsVisible ? "show" : "")"></div>

@code {
    [Parameter]
    public bool IsVisible { get; set; }

    // Additional parameters or methods can be added here if needed
}
